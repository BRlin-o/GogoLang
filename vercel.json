{
	"devCommand": "uvicorn main:app --host 0.0.0.0 --port 8000",
	"builds": [
	  {
		"src": "api/line_basic.py",
		"use": "@vercel/python"
	  }
	],
	"routes": [
	  {
		"src": "/(.*)",
		"dest": "api/line_basic.py"
	  }
	]
}